# 디자인 시스템: MoPrint (Momentary + Print)

## 개요

**MoPrint**는 스쳐 지나가는 소중한 순간(Momentary)을 인쇄(Print)하듯 따뜻하게 기록하고 간직하는 감성 다이어리 애플리케이션입니다. 전체적인 분위기는 **크림 톤의 따뜻한 종이 캔버스** (`{colors.canvas}` — #fffaf0) 배경 위에 부드러운 먹색 텍스트와 **아기자기한 3D 파스텔 점토 스타일 일러스트**(미니 폴라로이드 카메라, 프린터 롤, 보들보들한 구름, 하트 캐릭터 마스코트)가 어우러져 브랜드 감성을 전달합니다.

정형화된 그리드나 차가운 그라데이션 대신, MoPrint는 **포인트 파스텔 컬러의 일기 카드**를 활용하여 그날의 기분이나 그룹을 리듬감 있고 생동감 있게 표현합니다.

타이포그래피는 큰 크기의 디스플레이 헤드라인(예: 날짜 날인, 메인 타이틀)에 **Quicksand**(영문/숫자) 및 **고운 돋움(Gowun Dodum)**(한글) 서체를 사용하며, 자간을 약간 좁혀(-1px ~ -2px) 감성적이고 정돈된 Plain Black 느낌을 줍니다. 본문 서체로는 깨끗하고 가독성 좋은 **Inter** 및 기본 고딕 체를 사용합니다. 둥글둥글하고 친근한 서체 특성이 MoPrint에 무겁지 않은 아늑한 따뜻함을 더해줍니다.

**핵심 특징:**
- **따뜻한 종이 캔버스** (`{colors.canvas}` — #fffaf0): 고급 질감 종이를 연상시키는 아늑하고 크림빛이 도는 화이트 배경.
- **먹색 네이비** (`{colors.primary}` — #1a1a24): 따뜻한 푸른 빛이 도는 부드러운 블랙으로, 기본 텍스트 및 핵심 버튼에 사용.
- **5가지 파스텔 카드 색상**: 일기의 기분, 카테고리, 공유 그룹을 부드럽게 표현하는 따뜻한 파스텔 배색.
- **폴라로이드/티켓 디자인**: 인쇄된 영수증 롤, 폴라로이드 사진 프레임, 우표/스탬프 형태의 UI 디테일.
- **여유롭고 둥근 마감**: 버튼/입력창은 `{rounded.md}` (12px), 일반 카드는 `{rounded.lg}` (16px), 피드용 메인 일기 카드는 `{rounded.xl}` (24px) 반경을 적용하여 포근한 느낌 유지.
- **크림색 푸터**: 하단 영역 역시 어둡지 않은 따뜻한 크림색 (`{colors.surface-soft}` — #faf5e8)을 적용하고, 구름과 인쇄 롤이 어우러진 3D 지평선 일러스트를 배치.

## 색상 (Colors)

### 기분 및 그룹 포인트 카드
- **주요 잉크색 (Primary Ink)** (`{colors.primary}` — #1a1a24): 헤드라인, 핵심 액션 버튼, 활성화된 텍스트.
- **파스텔 핑크 (Pastel Pink)** (`{colors.brand-pink}` — #ffb7b2): 사랑스럽고 기쁘거나 설레는 순간용.
- **파스텔 민트/틸 (Pastel Teal)** (`{colors.brand-teal}` — #b5ead7): 평온하고 차분하며 상쾌한 순간용.
- **파스텔 라벤더 (Pastel Lavender)** (`{colors.brand-lavender}` — #c7ceea): 생각이 많거나 차분하고 감성적인 순간용.
- **Pastel Peach (파스텔 피치)** (`{colors.brand-peach}` — #ffdac1): 포근하고 아늑한 일상의 순간용.
- **Pastel Ochre (파스텔 옐로우/오커)** (`{colors.brand-ochre}` — #ffebd3): 밝고 활기차며 에너지가 넘치는 순간용.
- **Pastel Coral (파스텔 코랄)** (`{colors.brand-coral}` — #ff9aa2): 알림 배지, 경고 및 피드백 강조용.

### 화면 및 종이 질감
- **캔버스 (Canvas/따뜻한 종이)** (`{colors.canvas}` — #fffaf0): 기본 페이지의 밑바탕 배경색.
- **소프트 표면 (Surface Soft/린넨 화이트)** (`{colors.surface-soft}` — #faf5e8): 푸터 영역 및 입력 폼 패널 배경색.
- **카드 표면 (Surface Card/크림 카드)** (`{colors.surface-card}` — #f5f0e0): 비활성 탭, 리스트 백그라운드 포장용.
- **하이라인 (Hairline/부드러운 테두리)** (`{colors.hairline}` — #e5e0d0): 카드 테두리 및 입력창용 1px 경계선 색상.

### 텍스트
- **먹색 (Ink)** (`{colors.ink}` — #1a1a24): 메인 타이틀 및 높은 대비가 필요한 텍스트.
- **강한 본문 (Body Strong)** (`{colors.body-strong}` — #2a2a35): 강조된 본문 및 리스트 요약 텍스트.
- **기본 본문 (Body)** (`{colors.body}` — #4a4a55): 기본 일기 본문 텍스트.
- **Muted (흐린 텍스트)** (`{colors.muted}` — #7a7a85): 날짜 도장 정보, 보조 라벨, 힌트용 텍스트.
- **On Dark / On Saturated (반전 텍스트)** (`{colors.on-dark}` — #ffffff): 민트나 라벤더 카드 등 배경이 짙을 때의 가독성 확보용 화이트 텍스트.

## 타이포그래피 (Typography)

### 서체 구성
MoPrint는 부드럽고 손글씨 같은 다정함을 주기 위해 영문/숫자용 **Quicksand**와 국문용 **고운 돋움(Gowun Dodum)**을 조합해 사용합니다. UI의 명확성이 필요한 부분에는 맑은 고딕이나 **Inter**를 폴백 서체로 배치합니다.

### 계층 구조

| 토큰 | 크기 | 두께 | 줄높이 | 자간 | 사용 목적 |
|---|---|---|---|---|---|
| `{typography.display-xl}` | 48px | 600 | 1.1 | -1.5px | 히어로 메인 문구 ("순간을 인쇄하다") |
| `{typography.display-lg}` | 36px | 600 | 1.15 | -1.0px | 색션 타이틀, 날짜 도장 헤더 |
| `{typography.display-md}` | 28px | 500 | 1.2 | -0.5px | 일기 상세 보기 화면의 제목 |
| `{typography.display-sm}` | 22px | 500 | 1.3 | 0 | 리스트 카드 제목, 팝업 헤더 |
| `{typography.title-lg}` | 18px | 600 | 1.4 | 0 | 입력폼 항목 타이틀, 그룹 이름 |
| `{typography.body-md}` | 16px | 400 | 1.6 | 0 | 일기 본문 텍스트 (줄글 가독성 고려) |
| `{typography.body-sm}` | 14px | 400 | 1.5 | 0 | 부가 정보 기술, 타임스탬프 정보 |
| `{typography.caption}` | 12px | 600 | 1.4 | 1.0px | 공개여부 태그 (PRIVATE, PUBLIC, GROUP) |

## 레이아웃 및 여백 (Layout & Spacing)

### 여백 시스템
- **기본 단위:** 4px 배수.
- **토큰 정의:**
  - `{spacing.xs}`: 8px (배지 내부 여백)
  - `{spacing.sm}`: 12px (폼 입력란 사이 간격)
  - `{spacing.md}`: 16px (카드 내부 요소 여백)
  - `{spacing.lg}`: 24px (표준 카드 바깥 여백 및 카드 간 격자 간격)
  - `{spacing.xl}`: 32px (입력 패널 내부 여백)
  - `{spacing.section}`: 64px (페이지 최상단/최하단 수직 마진)

### 그리드 및 타임라인 구성
- **최대 콘텐츠 너비**: 1000px 중앙 정렬 (1단 피드 레이아웃의 편안한 정렬).
- **피드/리스트 구조**: 수직 방향으로 시간순(최신 일기가 위)으로 카드가 나열되는 타임라인 피드.
- **폴라로이드 카드 그리드**: 넓은 화면 해상도에서는 폴라로이드 형태의 다이어리가 2열 배치가 되는 반응형 구조 적용.

## 둥글기 (Shapes & Radii)

| 토큰 | 값 | 사용처 |
|---|---|---|
| `{rounded.sm}` | 8px | mood 아이콘 테두리, 소형 배지 |
| `{rounded.md}` | 12px | 텍스트 입력 칸, 일반 버튼 (`높이: 44px`) |
| `{rounded.lg}` | 16px | 폴라로이드 사진 테두리, 그룹 필터 탭 |
| `{rounded.xl}` | 24px | 일기 메인 카드, 다이어리 작성 모달/창 |
| `{rounded.pill}` | 9999px | 분류 필터 필(Pill) 디자인 |

## 컴포넌트 규격 (Components)

### 상단 네비게이션 (`top-nav`)
- **높이**: 64px.
- **배경**: 캔버스 색상 (`#fffaf0`)과 하단에 얇은 실선 테두리 (`#e5e0d0`).
- **레이아웃**: 
  - 좌측: **MoPrint** 시그니처 둥근 로고 타입.
  - 중앙: 필터 탭 ("나의 기록 (Private)", "모두의 순간 (Public)", "그룹 공간 (Groups)").
  - 우측: "일기 작성" 핵심 액션 버튼 (`button-primary`) 및 유저 프로필 아이콘.

### 버튼 (Buttons)
- **`button-primary`**: 잉크 네이비 배경 (`#1a1a24`), 글자색 크림 (`#fffaf0`), 높이 44px, 둥글기 `{rounded.md}`. 스탬프 도장을 꾹 찍은 듯한 직관적이고 또렷한 피드백 제공.
- **`button-secondary`**: 크림 배경에 1px 테두리 (`#e5e0d0`), 텍스트 잉크 네이비.
- **`button-cancel`**: 부드러운 코랄 배경 (`#ff9aa2`), 텍스트 잉크 네이비.

### 폴라로이드 일기 카드 (`diary-card-polaroid`)
- MoPrint의 아이덴티티를 가장 잘 보여주는 핵심 컴포넌트.
- **구조**:
  - 카드 배경은 5가지 파스텔 기분 컬러 (`brand-pink`, `brand-teal` 등) 중 하나를 순차 적용.
  - 내부에 하얀색 폴라로이드 프레임 카드(`{rounded.lg}`, 하얀 배경, 사진이 약간 비스듬하게 회전된 듯한 모티브 및 하단 낙서 영역)가 위치함.
  - 날짜는 도장을 찍은 듯한 스탬프 폰트로 렌더링 (예: `2026.06.07`).
  - 본문 글은 `{typography.body-md}`를 적용해 서정적이고 가독성 있게 표시.
  - 하단부에 공개 범위 배지(`Private`, `Group: Family`, `Public`)와 수정/삭제 등 관리 링크 배치.

### 다이어리 작성 창 (`diary-form`)
- 화면 우측에서 슬라이드인(Slide-in) 형태로 등장하거나, 둥근 모달창으로 열림.
- 실시간 폴라로이드 미리보기를 지원하여, 사용자가 사진을 등록하면 즉시 폴라로이드 프레임 내에 이미지가 얹어지는 것을 감성적으로 확인 가능.
- 입력 항목: 제목, 날짜, 이미지 선택(Canvas API를 이용해 최대 가로 800px로 압축된 JPEG 포맷으로 변환 저장), 본문 내용 작성, 공개 설정 (1단계는 `Private` 고정).

### 푸터 (`footer-print`)
- 따뜻하고 차분한 소프트 크림 톤 배경 (`#faf5e8`).
- 영수증 프린터기에서 종이가 주르륵 흘러나오는 아기자기한 3D 느낌의 그래픽과 "Every moment is worth printing (모든 순간은 인쇄할 가치가 있습니다)" 슬로건 배치.

## 권장 사항 및 금지 사항 (Do's and Don'ts)

### 권장 사항 (Do)
- 화면 전체 배경은 반드시 아늑하고 따뜻한 크림빛 화이트 (`#fffaf0`)로 지정합니다.
- 사진 업로드 시 LocalStorage가 꽉 차지 않도록 HTML Canvas를 활용해 용량을 대폭 리사이징 및 압축한 후 base64 변환하여 저장합니다.
- 다이어리 목록 렌더링 시 연속된 카드에 다른 파스텔 색상을 순환 적용하여 화사하고 경쾌한 리듬감을 제공합니다.
- 마우스 호버 및 버튼 터치 시 부드럽고 가볍게 튀어 오르는 바운시 애니메이션(`transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275)`)을 적용합니다.
- 1단계에서는 오직 **Private(나만 보기) 기능**과 작성 프로세스의 직관성에 집중합니다.

### 금지 사항 (Don't)
- 차갑고 푸른빛이 도는 쿨 그레이 계열 (`#f3f4f6`, `#e5e7eb`)을 배경이나 보더에 남발하지 않습니다.
- 압축 없이 원본 이미지를 그대로 로컬 스토리지에 박아 넣지 않습니다 (용량 초과 에러 방지).
- 직각 모서리(border-radius: 0)를 사용해 분위기를 깨지 않습니다. 무조건 아기자기하고 둥글게 마감합니다.
- 어두운 다크 모드 테마나 검은색 푸터를 임의로 덧붙이지 않습니다. MoPrint는 오직 따뜻한 종이 감성 테마만 유지합니다.
